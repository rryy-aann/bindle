// General utilities
// Example: formatting, string manipulation, math, time functions

export function formatXP(xp: number): string {
  return `${xp.toLocaleString()} XP`;
}

// Add more utilities as needed
